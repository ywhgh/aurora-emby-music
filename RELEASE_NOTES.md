## 版本说明

本版本继续加固音乐桥恢复播放稳定性。暂停后恢复播放时，如果音频元素里已经挂着旧音乐桥生成的 `/plugin-stream` 或 `/remote-stream` 地址，前端会先识别它是否属于当前音乐桥，不再直接沿用旧桥代理地址。

## 更新内容

- 新增通用音乐桥代理地址识别，覆盖 `/plugin-stream` 和 `/remote-stream`。
- 恢复播放前如果检测到旧桥代理地址，会触发当前音乐桥重新解析。
- 保留当前音乐桥地址的正常恢复路径，避免不必要的重载。
- 增加静态回归检查，确保暂停恢复时不会继续使用旧桥播放地址。
