## 版本说明

本版本继续收紧歌词逐字性能热路径。歌词 RAF 现在只更新当前可见的歌词面板，沉浸页播放时不会再每帧写入隐藏的正在播放页歌词 DOM，减少不必要的样式写入，让逐字填充更稳定。

## 更新内容

- `updateInlineLyricProgress` 支持按可见面板选择更新目标，避免隐藏歌词节点参与每帧刷新。
- 沉浸页 RAF 热路径不再写入普通歌词列表和当前歌词卡片的隐藏 DOM。
- 正在播放页仍保留歌词列表和当前歌词卡片的同步逐字刷新。
- 增加静态回归检查，防止后续把隐藏 DOM 写入重新带回歌词热路径。
