## 版本说明

本版本修复顶部栏双语逐字歌词在合成时间轴时的空格映射细节。英文原文包含空格且需要跟随翻译逐字时间时，顶部栏会跳过空格片段并保持空格后的字符时间继续递进，避免恢复或快进后出现局部错位。

## 更新内容

- 顶栏字符 timing 映射时跳过纯空格片段，空格仍显示但不参与逐字触发。
- 修复 `Hello world` 这类合成时间轴在空格后可能重新贴近行起点的问题。
- 浏览器回归检查新增顶部栏双语 timing samples，验证空格字符无 timing 且空格后的字符时间继续递进。
