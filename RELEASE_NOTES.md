## 0.93.167

### 版本说明
修正播放器样式弹窗的移动端样式作用范围与运行态验证：确保大半屏底部抽屉只在手机端生效，且不会被后续移动端通用弹窗规则覆盖。

### 更新内容
- 将播放器样式弹窗的大半屏底部面板规则移动到正确的 `max-width: 620px` 媒体查询内。
- 保证播放器样式弹窗优先覆盖通用移动端弹窗尺寸规则。
- 浏览器烟测新增运行态校验：手机端弹窗高度必须超过半屏，内容区必须可滚动且隐藏滚动条。
- 保留播放器主题/可视化样式的真实切换校验。

### 验证
- `npm run check:js`
- `npm run smoke`
- `npm run smoke:bridge`
- `BROWSER_SMOKE_RUN=1 npm run smoke:browser`
- `git diff --check`

## 0.93.166

### 版本说明
重做沉浸式播放器样式弹窗的手机端体验：弹窗改为大半屏底部抽屉，内容可滚动但隐藏滚动条，播放器主题与可视化音乐样式增加完整选中态和真实切换校验。

### 更新内容
- 手机端播放器样式弹窗改为大半屏底部面板，更接近沉浸式播放器风格。
- 弹窗内容支持纵向滚动，并隐藏滚动条，方便查看更多选项。
- 播放器主题/可视化样式选项增加暖白选中勾选态、按压反馈和高亮描边。
- 补完整主题视觉：流体光雾、舞台暖光会影响封面光影与背景氛围。
- 补完整可视化样式：暖白长波、柔光丝带、低频脉冲对应不同波形线宽、光晕和运动轨迹。
- 浏览器烟测新增真实点击切换验证，确保选项不是只显示不生效。

### 验证
- `npm run check:js`
- `npm run smoke`
- `npm run smoke:bridge`
- `BROWSER_SMOKE_RUN=1 npm run smoke:browser`
- `git diff --check`

## 0.93.165

### 版本说明
补强沉浸式封面当前歌词的切换动画稳定性：快速切歌、快进或歌词连续跳转时，旧动画清理定时器不会再提前移除新歌词动画，双语歌词显示会更稳定。

### 更新内容
- 当前歌词每次重播动画前会清理旧动画定时器。
- 歌词隐藏时同步清理动画定时器，避免残留回调影响下一句。
- 静态烟测增加动画定时器防回退保护。

### 验证
- `npm run check:js`
- `npm run smoke`
- `npm run smoke:bridge`
- `BROWSER_SMOKE_RUN=1 npm run smoke:browser`
- `git diff --check`

## 0.93.164

### 版本说明
优化沉浸式封面页的当前歌词显示：当歌词存在翻译时，封面下方不再只显示原文，而是尽量以两行展示原文与翻译，让用户在封面/可视化模式下也能看懂当前唱句。

### 更新内容
- 沉浸式封面下方当前歌词支持双语显示：第一行原文，第二行翻译。
- 无翻译歌词保持单句显示，并保留最多两行截断。
- 双语当前歌词增加独立暖白层级样式，翻译弱化但保持可读。
- 浏览器烟测新增移动端封面双语歌词校验。
- 歌词设置即时保存会取消待执行的延迟保存，避免重复写入本地存储。

### 验证
- `npm run check:js`
- `npm run smoke`
- `npm run smoke:bridge`
- `BROWSER_SMOKE_RUN=1 npm run smoke:browser`
- `git diff --check`

## 0.93.163

### 版本说明
补强移动端歌词设置保存可靠性：当用户拖动歌词字号后立刻切后台，页面隐藏时也会强制保存待写入的歌词设置，避免移动浏览器暂停定时器导致设置偶发丢失。

### 更新内容
- `visibilitychange` 进入后台时强制 flush 歌词设置保存队列。
- 保留播放进度后台强制保存与歌词动画冻结逻辑。
- 静态烟测更新为同时校验后台保存歌词设置与播放位置。

### 验证
- `npm run check:js`
- `npm run smoke`
- `npm run smoke:bridge`
- `BROWSER_SMOKE_RUN=1 npm run smoke:browser`
- `git diff --check`

## 0.93.162

### 版本说明
优化移动端歌词设置的保存性能：拖动歌词字号时界面仍然即时响应，但本地设置写入会延迟合并，减少同步存储写入导致的滑动卡顿风险，并在页面离开前强制保存，避免设置丢失。

### 更新内容
- 歌词字号拖动时不再每一格都同步写入 `localStorage`。
- 新增歌词设置保存合并队列，连续拖动时 220ms 内只保存最后一次结果。
- `pagehide` 与 `beforeunload` 会强制落盘歌词设置，保证移动浏览器切后台/关闭页面不丢设置。
- 静态烟测增加保存合并与离页落盘保护。

### 验证
- `npm run check:js`
- `npm run smoke`
- `npm run smoke:bridge`
- `BROWSER_SMOKE_RUN=1 npm run smoke:browser`
- `git diff --check`

## 0.93.161

### 版本说明
优化歌词设置拖动时的刷新策略，避免连续调整字体大小时反复触发歌词布局和滚动校准，降低移动端沉浸式歌词页面的抖动与卡顿风险。

### 更新内容
- 歌词字体设置的下一帧布局刷新改为可取消的合并刷新。
- 连续拖动字号滑块时只保留最后一次歌词布局校准，减少重复滚动定位。
- 静态烟测增加合并刷新保护，防止后续回退为无限排队的 `requestAnimationFrame`。

### 验证
- `npm run check:js`
- `npm run smoke`
- `npm run smoke:bridge`
- `BROWSER_SMOKE_RUN=1 npm run smoke:browser`
- `git diff --check`

## 0.93.160

### 版本说明
继续修复移动端沉浸式歌词设置的真实生效链路：字体大小或字体切换后立即刷新歌词布局，并在下一帧再次校准滚动位置，避免用户调完字号后当前歌词位置看起来没有变化或没有重新居中。

### 更新内容
- 歌词字体大小/字体切换后强制刷新当前歌词高亮与滚动定位。
- 增加下一帧二次歌词布局校准，保证 CSS 字号计算完成后重新居中当前行。
- 浏览器烟测新增“歌词设置字号变更后沉浸式歌词立即变大”的运行时校验。

### 验证
- `npm run check:js`
- `npm run smoke`
- `npm run smoke:bridge`
- `BROWSER_SMOKE_RUN=1 npm run smoke:browser`
- `git diff --check`

## 0.93.159

### 版本说明
修复移动端沉浸式播放页的歌词设置不生效、顶部歌名视觉未对齐和更多弹窗蓝色点击块残留问题，同时继续优化无逐字时间戳歌词的跟唱进度，让短词与长词的点亮节奏更贴近实际演唱。

### 更新内容
- 移动端沉浸式歌词字号改为读取歌词设置比例，小屏幕也保留字体调节效果。
- 顶部歌名改为小幅视觉偏移对齐全屏/退出按钮，浏览器烟测改为校验歌名字体本身的位置。
- 更多操作弹窗的焦点、按下、触摸高亮统一改为暖白反馈，彻底去掉蓝色点击块。
- 无逐字时间戳歌词改用按词长加权的兜底进度，减少“短词拖太久、长词提前唱完”的问题。

### 验证
- `npm run check:js`
- `npm run smoke`
- `npm run smoke:bridge`
- `BROWSER_SMOKE_RUN=1 npm run smoke:browser`
- `git diff --check`

## 0.93.158

### 版本说明

本版本修复手机沉浸式歌词页的返回交互：进入歌词页后，点击顶部歌名或歌手名会回到封面与音乐律动页面；点击歌词区空白处也会返回，但不会拦截歌词行本身的跳转播放。顶部歌名重新贴齐左右按钮顶端，并拉开歌名与歌手名的间距，让歌词页顶部不再拥挤。

### 更新内容

- 修复手机沉浸式歌词页事件绑定错误，避免点击返回逻辑引用不存在的处理函数。
- 顶部歌名/歌手名改为可点击返回入口，点击后返回封面、当前歌词和音乐可视化页面。
- 歌名/歌手名返回入口补充键盘可达性，支持 Enter/空格触发。
- 保留歌词行点击跳转播放能力，只有歌词区空白处才会触发返回。
- 手机歌词页顶部歌名重新与左上全屏、右上退出按钮顶端对齐。
- 增加歌名与歌手名的垂直间距，减少顶部信息拥挤感。
- 浏览器冒烟测试新增点击歌名/歌手名返回封面页的断言。

### 验证

```powershell
npm run check:js
npm run smoke
npm run smoke:bridge
$env:BROWSER_SMOKE_RUN='1'; $env:BROWSER_SMOKE_TIMEOUT_MS='120000'; $env:BROWSER_SMOKE_URL='http://127.0.0.1:5173/'; npm run smoke:browser
```

## 0.93.157

### 版本说明

本版本统一手机沉浸式播放界面的视觉语言：沉浸层不再使用红色状态色，弹窗、音质选中、下载选项、歌词设置、播放器样式和封面暖光都收敛为暖白色系。同时修正歌词页顶部歌名区域，歌名字号放大，并让歌名顶部与左上全屏、右上退出按钮顶端对齐，避免顶部信息看起来错位。

### 更新内容

- 沉浸式播放器封面兜底背景、封面光晕和舞台背景改为暖白/米白氛围，不再继承主界面的红色主题色。
- 沉浸式弹窗遮罩和卡片改为更统一的暖白暗色毛玻璃效果，弱化主页面弹窗割裂感。
- 沉浸式音质、下载、更多、歌词设置和播放器样式弹窗内的选中态统一为暖白色，不再出现红色、蓝色、紫色等音质染色。
- 手机沉浸式弹窗改为底部暖白毛玻璃抽屉样式，列表可滚动但隐藏滚动条。
- 手机沉浸式歌词页顶部歌名放大，歌名与艺人信息顶端和全屏/退出按钮对齐。
- 修复顶部歌名进场动画自带下移偏移的问题，进入歌词页时不会先低一截再回正。
- 静态检查新增沉浸式暖白化和顶部歌名字号/对齐守卫。
- 浏览器冒烟测试新增手机歌词页顶部歌名字号和按钮顶端对齐断言。

### 验证

```powershell
npm run check:js
npm run smoke
npm run smoke:bridge
$env:BROWSER_SMOKE_RUN='1'; $env:BROWSER_SMOKE_TIMEOUT_MS='120000'; $env:BROWSER_SMOKE_URL='http://127.0.0.1:5173/'; npm run smoke:browser
```

## 0.93.156

### 版本说明

本版本修正手机沉浸式播放器的全屏入口位置，并彻底收口沉浸式按钮弹窗的层级问题：从沉浸页打开的下载、音质、更多、歌词设置和播放器样式不再像主页面弹窗一样压在主界面层，而是以暖白暗色毛玻璃风格覆盖在沉浸播放层上。同时封面页在音乐可视化上方新增“当前歌词”提示，只做整句淡入，不参与逐字播放。

### 更新内容

- 手机沉浸式播放页全屏按钮固定在左上角显示。
- 移除底部工具栏中的全屏入口，底部工具栏恢复为收藏、纯享、音质、下载、更多等播放工具。
- 进入歌词视图后，全屏按钮仍保留在左上角，右上角退出按钮保持可见。
- 顶部折叠空心点右移，不再占用左上角全屏入口位置。
- 修复沉浸式底部按钮打开的弹窗层级，下载、音质、更多、歌词设置和播放器样式都显示在沉浸式界面上层。
- 沉浸式弹窗改为暖白暗色毛玻璃卡片，弱化普通主页面白卡片割裂感。
- 手机封面页在音乐可视化上方新增当前歌词提示，非逐字播放，只随当前歌词整句淡入切换。
- 浏览器冒烟测试同步断言手机和窄屏手机视口下，全屏按钮位于顶部左侧且底部不再重复显示。
- 浏览器冒烟测试新增沉浸式弹窗层级、暗色风格和当前歌词位置断言。

### 验证

```powershell
npm run check:js
npm run smoke
npm run smoke:bridge
$env:BROWSER_SMOKE_RUN='1'; $env:BROWSER_SMOKE_TIMEOUT_MS='120000'; $env:BROWSER_SMOKE_URL='http://127.0.0.1:5173/'; npm run smoke:browser
```

## 0.93.155

### 版本说明

本版本继续优化双语逐字歌词和手机沉浸播放体验：当某些歌词只有原文逐字时间轴、翻译没有逐字时间轴时，播放器会根据翻译词长自动分配逐字进度，而不是把整句机械等分；同时音乐可视化改为读取真实频谱/RMS 能量，并补齐沉浸播放下载、音质、更多、歌词设置和播放器样式弹窗。手机歌词页现在会把歌名/歌手动画移动到顶部、保留右上角退出按钮，并把全屏状态图标放到底部左侧，整体操作更符合沉浸播放逻辑。

### 更新内容

- 双语歌词缺少翻译逐字时间轴时，合成逐字进度改为按词长权重分配。
- 保留源歌词逐字数量和目标显示词数量一致时的逐词映射，避免破坏已有精确时间轴。
- 手机沉浸页音乐可视化从时域弱采样升级为频谱数据 + RMS/Peak 能量包络驱动，波形幅度、光晕和流动轨迹会跟随真实音频能量变化。
- 可视化继续保留 `captureStream() -> MediaStreamSource` 旁路采样，不回退到会接管共享 `<audio>` 输出的 `MediaElementSource`，避免再次引入无声问题。
- 修复手机沉浸播放加载环被 `transform: rotate(0deg) !important` 锁死的问题，加载态会正常旋转。
- 修复沉浸播放页下载、音质和更多按钮点击后没有弹窗的问题，浏览器冒烟测试改为真实点击验证。
- 更多菜单新增“歌词设置”，支持调整歌词字体大小、字体、歌词时间偏移、歌词跟随播放滚动和自动展示沉浸式歌词；跟随播放默认开启，自动展示默认关闭。
- 更多菜单新增“播放器样式”，皮肤设置移动到这里，支持选择播放器主题和可视化音乐样式，并持久保存。
- 手机沉浸歌词页左下角新增全屏状态图标，进入全屏后图标带动效并切换为退出全屏图标；右上角退出按钮保持可见。
- 进入沉浸式歌词时，歌名与歌手会动画上移到顶部区域，专辑名不再显示；退出歌词视图时标题向下退场。
- 沉浸播放整页进入和退出新增轻量淡入/淡出动效，不改变桌面主布局。
- 浏览器冒烟测试新增长短词差异明显的双语逐字场景，验证短词会更早完成，长词承接剩余演唱时长。
- 浏览器冒烟测试新增音乐可视化能量场景，验证静音不会被误判为 live，强节拍会推动更大的波形和光晕。
- 移动端冒烟测试增强播放加载态、全屏图标、顶部标题、播放器样式弹窗和歌词设置弹窗断言。
- 冒烟状态采集新增逐字开始/结束时间，方便后续继续定位逐字同步细节。

### 验证

```powershell
npm run check:js
npm run smoke
npm run smoke:bridge
$env:BROWSER_SMOKE_RUN='1'; $env:BROWSER_SMOKE_TIMEOUT_MS='120000'; $env:BROWSER_SMOKE_URL='http://127.0.0.1:5173/'; npm run smoke:browser
```

## 0.93.154

### 版本说明

本版本修正手机沉浸播放器歌曲信息下方的音质展示：音质区域只显示实际档位值，不再重复出现“音质”文字，让标题、歌手/专辑和音质信息的层级更干净。

### 更新内容

- 手机沉浸页音质信息从“音质 + 档位”调整为只显示档位值。
- 浏览器冒烟测试同步覆盖手机和窄屏手机视口，确保音质文案不再带“音质”前缀。
- 移动端沉浸布局相关断言统一使用手机视口前缀匹配，避免窄屏检查漏掉同类问题。

### 验证

```powershell
npm run check:js
npm run smoke
npm run smoke:bridge
$env:BROWSER_SMOKE_RUN='1'; $env:BROWSER_SMOKE_TIMEOUT_MS='120000'; $env:BROWSER_SMOKE_URL='http://127.0.0.1:5173/'; npm run smoke:browser
```

## 0.93.153

### 版本说明

本版本继续优化逐字歌词流畅度：在 RAF 高频播放路径中加入轻量的单调进度保护，避免音频时钟出现几十毫秒级回抖时，当前字的高亮进度出现肉眼可见的倒退、闪回和忽快忽慢。手动 seek、歌词偏移调整、切歌和强制刷新仍然允许正常回退，不影响校准逻辑。

### 更新内容

- 逐字歌词 RAF 热路径新增 `monotonic` 进度保护，同一个播放窗口内不会接受更低的字进度百分比。
- 主界面歌词焦点、歌词列表和沉浸式歌词共用同一套防抖路径，双语歌词分组也保持独立缓存。
- 保留 seek、offset、换行、重渲染等非 RAF 路径的正常回退能力，避免防抖影响用户校准。
- 浏览器冒烟测试新增歌词抖动场景，覆盖 `0.70s -> 0.66s` 这类微回抖和跨字边界回抖。
- 静态 smoke 检查适配带 options 的逐字窗口更新函数，继续约束不能退回全量逐字扫描。

### 验证

```powershell
npm run check:js
npm run smoke
npm run smoke:bridge
$env:BROWSER_SMOKE_RUN='1'; $env:BROWSER_SMOKE_TIMEOUT_MS='120000'; $env:BROWSER_SMOKE_URL='http://127.0.0.1:5173/'; npm run smoke:browser
```

## 0.93.152

### 版本说明

本版本继续修正手机沉浸播放器的细节问题：中间封面、歌曲信息和波形重新按同一条中心轴收敛，播放按钮彻底压掉移动端蓝色点击高亮，播放加载态替换为更干净的暖白环形扫描，同时加宽底部进度与控制区的上下间距，让手机端看起来不再挤、不再偏。

### 更新内容

- 手机沉浸页封面视图增加最终居中约束，封面、歌名/歌手专辑、音质和波形统一按视口中心对齐。
- 将波形宽度从视口宽度约束改为父容器宽度约束，避免小屏被左右裁切后产生视觉偏移。
- 播放按钮和沉浸式图标按钮统一禁用移动端 tap 高亮、`accent-color` 和默认 active 背景，只保留轻微缩放/亮度反馈。
- 播放加载动画从三段竖条替换为暖白色细线环形扫描，减少和音乐波形视觉混在一起的问题。
- 手机沉浸页中间区域、歌曲信息、波形和底部控制区的上下间距进一步加宽，小屏断点同步保留间距。
- 浏览器冒烟测试新增 360px 窄屏档，并断言播放按钮 tap 高亮透明、加载态使用新环形动画。

### 验证

```powershell
npm run check:js
npm run smoke
npm run smoke:bridge
$env:BROWSER_SMOKE_RUN='1'; $env:BROWSER_SMOKE_TIMEOUT_MS='120000'; $env:BROWSER_SMOKE_URL='http://127.0.0.1:5173/'; npm run smoke:browser
```

## 0.93.151

### 版本说明

本版本继续收紧移动端沉浸播放器的实际使用体验：中间封面、歌曲信息和波形重新锁定到手机视口中心线，底部播放控制改为更松的上下间距，播放按钮在手机端改成纯图标并去掉点击时的蓝色背景，同时新增音质/下载弹窗和收藏实心反馈，保证功能按钮不是摆设。

### 更新内容

- 手机沉浸页中间区域增加最终移动端对齐层，封面、歌名/歌手专辑、音质和波形统一按视口中心居中。
- 手机沉浸页播放按钮改为纯图标按钮，禁用蓝色点击高亮和背景闪烁，只保留轻微缩放反馈。
- 播放加载态从圆环 spinner 改为暖白色三段律动条，和音乐播放器视觉更一致。
- 底部进度条、主播放按钮行和工具按钮行的上下间距加宽，小屏幕也保留更舒服的呼吸感。
- 专辑封面继续使用放大裁剪 `object-fit: cover`，减少封面区域露底的问题。
- 播放顺序图标统一为列表播放样式，电脑端和沉浸端保持一致。
- 收藏按钮支持真实状态反馈，收藏后手机和电脑沉浸控制都会显示实心心形。
- 音质按钮会打开音质切换弹窗，点击外部区域关闭。
- 下载按钮会打开下载选项弹窗，弹窗内可选择下载音质。
- 手机歌词页顶部按钮折叠为左侧空心点，点击空心点恢复皮肤、全屏和退出按钮。
- 浏览器冒烟测试新增手机沉浸页播放按钮透明背景、控制区间距、音质展示、顶部折叠、弹窗和收藏状态断言。

### 验证

```powershell
npm run check:js
npm run smoke
npm run smoke:bridge
$env:BROWSER_SMOKE_RUN='1'; $env:BROWSER_SMOKE_TIMEOUT_MS='120000'; $env:BROWSER_SMOKE_URL='http://127.0.0.1:5173/'; npm run smoke:browser
```

## 0.93.150

### 版本说明

本版本修复移动端沉浸播放器的两类核心体验问题：音乐可视化改为不接管音频输出的旁路真实采样，能跟随当前音乐能量律动，同时避免再次引发播放无声；移动端沉浸页重新校准顶部按钮、中间封面/歌名/歌手专辑/波形/歌词的中心轴，让界面不再向右偏。

### 更新内容

- 音乐可视化新增 `audio.captureStream()` 旁路采样，使用 `MediaStreamSource -> Analyser` 读取真实音频能量，不再使用会接管 `<audio>` 输出的 `MediaElementSource`。
- 音乐桥和同源音频会自动开启可采样的 `crossOrigin="anonymous"`，切歌或卸载音频时释放旧分析器，降低无声和旧采样残留风险。
- 波形计算从单点采样改为窗口 RMS/峰值能量采样，律动幅度更贴近真实音乐，不再像固定假动画。
- 手机沉浸页顶部不再显示歌曲名称，顶部按钮贴近顶部安全区对齐。
- 中间歌名、歌手/专辑字体放大，并拉开上下间距；封面、标题区、波形和歌词列表统一居中。
- 专辑封面背景改为更柔和的暖白光晕，波形整体下移，底部主控制和工具按钮横向间距加大。
- 移除沉浸页按钮的蓝色/白色点击背景反馈，保留轻微缩放和颜色变化。
- 浏览器冒烟测试新增移动端中轴断言，覆盖封面、标题区、波形居中和顶部标题隐藏。

### 验证

```powershell
npm run check:js
npm run smoke
npm run smoke:bridge
$env:BROWSER_SMOKE_RUN='1'; $env:BROWSER_SMOKE_TIMEOUT_MS='90000'; $env:BROWSER_SMOKE_URL='http://127.0.0.1:5173/'; npm run smoke:browser
```

## 0.93.149

### 版本说明

本版本继续优化逐字歌词体验，并加入紧急播放稳定性修复：针对部分歌词源会返回重复或过密逐字时间戳的问题，播放器会在渲染逐字分组时自动铺开最小时间间隔，避免多个字在同一瞬间一起跳完；同时禁用沉浸页波形可视化对共享 audio 元素的 Web Audio 接管，避免部分浏览器出现有进度但无声音的情况。

### 更新内容

- 逐字歌词时间轴新增最小间隔正规化，重复时间戳会自动按 80ms 递进展开。
- 保留原有 `clip-path` 高亮热路径，不改成逐帧宽度布局写入，避免移动端出现新的绘制压力。
- 同一套正规化逻辑覆盖沉浸歌词、主歌词列表和当前播放歌词焦点。
- 紧急修复沉浸页波形可视化接管 `<audio>` 输出链导致部分浏览器播放无声的问题。
- 波形可视化暂时使用时间驱动的安全兜底，不再创建 `MediaElementSource`，优先保证音乐播放声音稳定。
- 增加真实 Chrome 冒烟测试场景，覆盖多个逐字时间戳完全相同的歌词，确认不会瞬间跳到后续字。

### 验证

```powershell
npm run check:js
npm run smoke
npm run smoke:bridge
$env:BROWSER_SMOKE_RUN='1'; $env:BROWSER_SMOKE_URL='http://127.0.0.1:5173/'; npm run smoke:browser
```

## 0.93.148

### 版本说明

本版本聚焦移动端沉浸播放器的中间视觉区：彻底移除旧的柱状律动条，改为更宽、更明显的横向流动波形。波形优先使用 Web Audio API 的时域采样跟随真实音乐振幅，取不到音频数据时才使用安静的流动兜底，整体保持暖白色音乐氛围，不再保留偏红柱状条的割裂感。

### 更新内容

- 移动端沉浸播放器默认封面区改为 SVG 横向波形，包含主线、柔光层、填充雾面和从左到右的流动高光。
- Web Audio 可视化从频谱柱改为 `getByteTimeDomainData()` 时域波形采样，律动更接近真实播放状态。
- 暂停或未播放时回落到低幅度安静波形，避免中间区域变成僵硬直线。
- 清理移动端旧 `.immersive-visualizer` 柱状条结构和样式，避免新旧视觉方案同时残留。
- 保留手机沉浸页点击中间区域切换封面/歌词的原逻辑，同时同步移动端收藏、纯享、音质、下载和更多操作入口。
- 增强真实 Chrome 冒烟测试，要求手机端波形足够宽高、使用平滑曲线路径，并确认旧柱状条 DOM 数量为 0。

### 验证

```powershell
npm run check:js
npm run smoke
npm run smoke:bridge
$env:BROWSER_SMOKE_RUN='1'; $env:BROWSER_SMOKE_URL='http://127.0.0.1:5173/'; npm run smoke:browser
```

## 0.93.147

### 版本说明

本版本聚焦播放稳定性和逐字歌词流畅度：优化 RAF 逐字进度使用的高精度歌词时钟，轻微音频时间漂移会渐进校正，避免 timeupdate 粗粒度同步造成逐字填充突然跳动；同时强化音乐桥缓存落盘和重新进入应用后的外部音源恢复链路，减少重新搜索才能播放的问题。

### 更新内容

- 逐字歌词时钟新增软校正机制，小幅漂移按比例拉回，不再每次都硬重置播放基准。
- 增加硬重同步阈值，seek、卡顿或真实大漂移时仍会快速追上音频时间。
- 音乐桥插件缓存位于临时目录时会立即落盘，降低重进应用后外部音源缓存丢失的概率。
- 沉浸式播放右上工具补齐音质、下载当前音乐和更多操作入口，下载会优先解析当前可用播放源。
- 沉浸式皮肤按钮文案统一为“皮肤样式”，线性图标和沉浸式图标加粗，提高鼠标经过和按钮识别度。
- 扩展静态、音乐桥和真实 Chrome 冒烟测试，覆盖软漂移校正、避免硬跳、硬漂移重同步、音乐桥缓存落盘和重进播放场景。
